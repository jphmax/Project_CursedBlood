using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnerScripts : MonoBehaviour
{
    //private Vector3[] position = new Vector3[50];
    // Start is called before the first frame update
    void Start()
    {
        /*Debug.Log(transform.childCount);
        for (int i = 0; i < transform.childCount; i++)
        {
            Debug.Log(transform.GetChild(i).GetComponent<MeshRenderer>().transform.position);
        }*/
        /*foreach (Transform child in transform)
        {
            Debug.Log(child.gameObject.GetComponent<MeshRenderer>().transform.position);
        }*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
